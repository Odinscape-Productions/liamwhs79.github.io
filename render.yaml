databases:
  - name: pulsestation-db
    plan: free
    databaseName: pulsestation
    user: pulsestation

services:
  - type: web
    name: pulsestation-community
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/status
    autoDeployTrigger: commit
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: pulsestation-db
          property: connectionString
      - key: PSN_SYNC_MODE
        value: psn-api
      - key: PSN_AUTO_SYNC
        value: "true"
      - key: PSN_NPSSO
        sync: false
